"""Phase 09 B1: the automated eval harness.

Runs the labeled fixtures in `sample-invoices.json` through the REAL
`DecisionPipeline` in-process (stub adapter, no docker/compose), scores
route accuracy, and writes `eval/REPORT.md`. See `eval/run_eval.py`.
"""
