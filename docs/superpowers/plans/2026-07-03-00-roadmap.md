# ApprovalFlow — Implementation Roadmap

> Master plan index. One plan file per phase; each phase ends with working, testable software,
> merged to `main` via PR (GitHub Flow, D3). Spec: `docs/superpowers/specs/2026-07-03-approvalflow-design.md`.
> Deadline: `main` frozen EOD **2026-07-12**.

| # | Phase (plan file) | Delivers | Day budget |
|---|---|---|---|
| 01 | Foundation (`…-01-foundation.md`) | git repo + monorepo layout, `afcommon` (JSON logs w/ correlation id, CAS/state helpers, topics), compose skeleton (Redis, placement, intake stub + sidecar, health-gated), Dapr components, CI (ruff+pytest) | 03–04/07 |
| 02 | ✅ Intake service (done 05/07) | invoice models (integer cents), `POST /invoices` 202+tracking id, `GET /invoices/{id}`, `PUT` resubmit, status state machine driven by events, dashboard counters, `invoice.submitted` publishing | 04–05/07 |
| 03 | ✅ Decision service (done 06/07) | Gate 1 fingerprint CAS, Gate 2 validators (FX/math/receipt/vendor/missing-info/TRAVEL-02), `DecisionAgent` port + `stub` + `handrolled` (OpenRouter, retries, AGENT-UNAVAILABLE), router + D-012 posture (category caps, trust tier), config in state + `PUT /config/thresholds`, `malicious_stub` M12 suite, trust-history subscriber | 05–07/07 |
| 04 | ✅ Approval service (done 07/07) | escalation records + `queue:pending` CAS index, `GET /queue`, `POST /verdict` (CAS-idempotent), needs_info flow, restart-survival test (M11) | 07/07 |
| 05 | ✅ Payment service (done 08/07) | saga step-marker, budget seeding, ETag CAS reservation (INV-1014A/B), mock provider + double-gated failure injection, compensation, crash-resume, M12 ceiling re-check | 08/07 |
| 06 | ✅ Audit + Notification (done 09/07) | audit-svc append-only trail on Postgres (D-017), F9 trail + F10 ceiling-compliance, Dapr-invoked by intake (`?trail=true`, M5 sync leg); vendored task3 notification service (subscriber, afcommon logging, healthz, seeding off) | 09/07 |
| 07 | ✅ Gateway + UI (done 09/07) | nginx single entry (:8080) routes `/api/*`, `limit_req` rate limiting, serves the SPA (M6); React SPA: submit, status+trail, approver queue, dashboard, thresholds, F10 compliance (M7) | 09/07 |
| 08 | ✅ Verification + docs (done 09/07) | D5 `make verify` (journeys A–D + anti-cheese, 7/7 PASS), ARCHITECTURE.md (Mermaid: component/sequence/payment-compensation), README final pass, OpenAPI reachability verified live (all six `/docs`+`/openapi.json` 200), demo script; `docs/SUBMISSION-CHECKLIST.md` for Daniel's hand-authored deliverables (ADRs, PRODUCT-DILEMMA.md) | 10–11/07 |
| 09 | Extras (only if 01–08 green) | in order: N6 explicit layers → N1 JWT roles → N5 RAG (BM25) → B1 eval harness + own fixtures | 11–12/07 |

**Buffer:** 12/07 is reserved for demo recording, final README pass, freezing `main`.
**Rule:** a phase is done only when its tests pass in CI and the compose stack still comes up clean.
**Manual step (Daniel, after phase 01):** create the private GitHub repo, push, add contributors
`alonf`, `VenyaBrodetskiy`, `holohup`, `milaShurupova`; write ADRs in `docs/adr/` from `decisions.md` as phases land (D2 — personal authorship).
